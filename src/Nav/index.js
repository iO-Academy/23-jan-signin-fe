

const Nav = () => {
    const handleClick = () => {
        //back to homepage
    }

    return (
        <div className="px-5 py-5 flex justify-end">
        <a onClick={handleClick} className="text-4xl h-24">&lt;&lt;Back to Homepage</a>
        </div>
    )
}

export default Nav;
